"""
Study[S]ync Agents Package

This package contains the three main agents of the system:
1. Planner Agent (מתכנן) — creates the study plan
2. Visualizer Agent (סימולטור) — generates interactive simulations
3. Tester Agent (בוחן) — generates tests and quizzes

Each agent lives in its own sub-package and exposes a clean entry point.
"""
